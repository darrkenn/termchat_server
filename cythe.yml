image: darrkenn/cythe-elixir:latest
track: main

steps:
  - name: Checkout repo
    use: cythe-checkout
  - name: Create test path
    run: mkdir test
  - name: Mix deps.get
    run: mix deps.get
  - name: Mix tests
    run: mix test
